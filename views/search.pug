doctype html
html
head
    link(rel="stylesheet" href="../css/style.css")
    script(src="https://code.jquery.com/jquery.js")

    title best app
body
-let User = {loggedOn: true}
    if User.loggedOn 
    
        include includes/nav

        .search_box 
            input(class="form" id="searchForm" type="text")
            select(id="searchCategory")
                option(value="movie") Movies
                option(value="book") Books
                option(value="podcast") Podcasts
            button(class="submit" id="search" type="submit") Search


    else
        a.log_in(href="login") Log In
        a.sign_up(href="signup") Sign Up
        br

        h1.intro Your Next Binge!

        .search_box 
            input(class="form" id="searchForm" type="text")
            select(id="searchCategory")
                option(value="movie") Movies
                option(value="book") Books
                option(value="podcast") Podcasts
            button(class="submit" id="search" type="submit") Search Reviews

script
    include ../public/js/search.js